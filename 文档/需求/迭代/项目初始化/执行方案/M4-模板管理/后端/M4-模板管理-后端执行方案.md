# M4 模板管理 - 后端执行方案

> **依赖**：M1（认证鉴权）、M5（系统管理-菜单）
> **涉及表**：`t_project_template`、`t_function_module`、`t_layout_rule`、`t_template_function`、`t_template_layout`、`t_feature`、`t_feature_template`
> **关键模块**：`pick-up-service`、`pick-up-api`

---

## 1. 模板创建完整流程

```
POST /api/v1/admin/templates
  │
  ├── TemplateController.create(@Valid TemplateRequest)
  │     └── @RequirePermission("template:write")
  │
  └── TemplateServiceImpl.createTemplate(request)
        │
        ├── ① 校验 templateCode 唯一性
        │     templateRepository.existsByCode(request.templateCode)
        │     └── true → throw ConflictException("编码重复")
        │
        ├── ② 校验关联模块存在
        │     moduleIds = request.moduleIds
        │     existingCount = functionModuleRepository.countByIdIn(moduleIds)
        │     └── existingCount != moduleIds.size → throw NotFoundException
        │
        ├── ③ @Transactional 创建
        │     ├── template = projectTemplateRepository.save(template)
        │     ├── templateFunctionRepository.saveAll(
        │     │     moduleIds.map(m -> TemplateFunction(templateId, moduleId, isRequired))
        │     │ )
        │     └── templateLayoutRepository.saveAll(
        │     │     layoutIds.map(l -> TemplateLayout(templateId, layoutId, isDefault))
        │     │ )
        │
        ├── ④ 清除 Redis 缓存
        │     redisTemplate.delete("template:list:all")
        │     redisTemplate.delete("template:detail:" + templateId)
        │
        └── ⑤ 返回 TemplateResponse
```

## 2. 模板复制流程

```
POST /api/v1/admin/templates/{id}/copy
  │
  └── TemplateServiceImpl.copyTemplate(sourceId)
        │
        ├── ① 查询源模板
        │     source = projectTemplateRepository.findById(sourceId)
        │
        ├── ② 生成新编码
        │     newCode = source.templateCode + "_copy_" + timestamp
        │
        ├── ③ @Transactional
        │     ├── INSERT INTO t_project_template (clone) RETURNING id
        │     ├── INSERT INTO t_template_function
        │     │     SELECT uuid_generate_v4(), newId, module_id, is_required, sort_order
        │     │     FROM t_template_function WHERE template_id = sourceId
        │     └── INSERT INTO t_template_layout
        │           SELECT uuid_generate_v4(), newId, layout_id, is_default
        │           FROM t_template_layout WHERE template_id = sourceId
        │
        └── ④ 清除缓存 + 返回新模板
```

## 3. 模块-排版关联全量替换

```
PUT /api/v1/admin/templates/{id}/modules
  Body: { modules: [{ moduleId, isRequired, sortOrder }, ...] }
  │
  └── TemplateServiceImpl.assignModules(templateId, modules)
        │
        ├── ① 校验模板存在
        ├── ② 校验所有模块 ID 有效
        │
        └── ③ @Transactional
              ├── DELETE FROM t_template_function WHERE template_id = ?
              └── BATCH INSERT INTO t_template_function
```

## 4. 核心查询 SQL

```sql
-- 1. 模板详情（含关联模块 + 排版）
SELECT t.*,
       (SELECT json_agg(json_build_object(
            'moduleId', fm.id, 'moduleName', fm.module_name, 'moduleKey', fm.module_key,
            'domain', fm.domain, 'isRequired', tf.is_required
        )) FROM t_template_function tf JOIN t_function_module fm ON tf.module_id = fm.id
        WHERE tf.template_id = t.id) AS modules,
       (SELECT json_agg(json_build_object(
            'layoutId', lr.id, 'layoutName', lr.layout_name, 'layoutType', lr.layout_type,
            'isDefault', tl.is_default
        )) FROM t_template_layout tl JOIN t_layout_rule lr ON tl.layout_id = lr.id
        WHERE tl.template_id = t.id) AS layouts
FROM t_project_template t WHERE t.id = ?;

-- 2. 功能模块树查询（递归 CTE）
WITH RECURSIVE feature_tree AS (
    SELECT id, parent_id, feature_name, feature_key, description, icon,
           0 AS depth, feature_name AS path
    FROM t_feature WHERE parent_id IS NULL AND status = 'ACTIVE'
    UNION ALL
    SELECT f.id, f.parent_id, f.feature_name, f.feature_key, f.description, f.icon,
           ft.depth + 1, ft.path || ' > ' || f.feature_name
    FROM t_feature f
    JOIN feature_tree ft ON f.parent_id = ft.id
    WHERE f.status = 'ACTIVE'
)
SELECT * FROM feature_tree ORDER BY path;

-- 3. 功能模块搜索
SELECT * FROM t_function_module
WHERE is_active = TRUE
  AND (:domain IS NULL OR domain = :domain)
  AND (:keyword IS NULL OR module_name ILIKE '%' || :keyword || '%'
       OR description ILIKE '%' || :keyword || '%')
ORDER BY is_core DESC, sort_order;

-- 4. 更新模板-模块关联（全量替换）
BEGIN;
    DELETE FROM t_template_function WHERE template_id = ?;
    INSERT INTO t_template_function (id, template_id, module_id, is_required, sort_order)
    VALUES (?, ?, ?, ?, ?), ...;
COMMIT;
```

## 5. 接口清单

### 模板管理

| METHOD | PATH | 权限 | 说明 |
|--------|------|------|------|
| GET | `/api/v1/admin/templates` | `template:view` | 模板列表 |
| GET | `/api/v1/admin/templates/{id}` | `template:view` | 模板详情 |
| POST | `/api/v1/admin/templates` | `template:write` | 创建模板 |
| PUT | `/api/v1/admin/templates/{id}` | `template:write` | 更新模板 |
| DELETE | `/api/v1/admin/templates/{id}` | `template:write` | 删除模板 |
| POST | `/api/v1/admin/templates/{id}/copy` | `template:write` | 复制模板 |
| PUT | `/api/v1/admin/templates/{id}/modules` | `template:write` | 更新关联模块 |
| PUT | `/api/v1/admin/templates/{id}/layouts` | `template:write` | 更新关联排版 |

### 功能模块管理

| METHOD | PATH | 权限 | 说明 |
|--------|------|------|------|
| GET | `/api/v1/admin/modules` | `module:view` | 模块列表 |
| POST | `/api/v1/admin/modules` | `module:write` | 创建模块 |
| PUT | `/api/v1/admin/modules/{id}` | `module:write` | 更新模块 |
| PUT | `/api/v1/admin/modules/batch-toggle` | `module:write` | 批量启停 |
| GET | `/api/v1/admin/modules/export` | `module:view` | 导出 Excel |
| POST | `/api/v1/admin/modules/import` | `module:write` | 导入 Excel |

### 排版规则管理

| METHOD | PATH | 权限 | 说明 |
|--------|------|------|------|
| GET | `/api/v1/admin/layouts` | `layout:view` | 排版列表 |
| POST | `/api/v1/admin/layouts` | `layout:write` | 创建排版 |
| PUT | `/api/v1/admin/layouts/{id}` | `layout:write` | 更新排版 |

### 功能管理（Feature 树）

| METHOD | PATH | 权限 | 说明 |
|--------|------|------|------|
| GET | `/api/v1/admin/features/tree` | `feature:view` | 功能树 |
| POST | `/api/v1/admin/features` | `feature:write` | 创建节点 |
| PUT | `/api/v1/admin/features/{id}` | `feature:write` | 更新节点 |
| PUT | `/api/v1/admin/features/batch-toggle` | `feature:write` | 批量启停 |
| PUT | `/api/v1/admin/features/batch-move` | `feature:write` | 批量移动 |

## 6. 功能管理树构建

```
FeatureServiceImpl.getTree()
  │
  ├── featureRepository.findAll( Sort.by("sortOrder") )
  └── 内存构建树: Map<parentId, List<Feature>> 递归

FeatureServiceImpl.batchMove(ids, newParentId)
  ├── 校验 newParentId 存在
  ├── 校验不形成循环引用（自引用/间接引用）
  └── UPDATE ... SET parent_id = ? WHERE id IN (?)
```

## 7. 用户端查询接口

| METHOD | PATH | 权限 | 说明 |
|--------|------|------|------|
| GET | `/api/v1/templates` | 已认证 | 可用模板列表（仅启用） |
| GET | `/api/v1/templates/{id}` | 已认证 | 模板详情 |
| GET | `/api/v1/templates/{id}/modules` | 已认证 | 模板关联功能项 |
| GET | `/api/v1/templates/{id}/layouts` | 已认证 | 模板关联排版方案 |
| GET | `/api/v1/modules/available` | 已认证 | 可选功能项库 |
| GET | `/api/v1/layouts/available` | 已认证 | 可选排版规则库 |
